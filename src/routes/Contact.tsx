import React from "react";
import { Map } from  '../parts';
import { Ohta, Sumomo } from '../images';

const devPath = "/projects/yodo";

const Contact = props => {
    return(
    <main className="contact__container page__container">
        <h1>This is contact page.</h1>
    </main>
    )
};
export default Contact;